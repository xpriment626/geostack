{
  "name": "geostack-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Geostack (GEO + substack) — executable-runtime port of the InstaLetter 5-agent newsletter pipeline. Mastra agents, OpenRouter-direct, per-agent model config, with a cost-tracking layer for cost:capability trials.",
  "scripts": {
    "worker": "tsx src/coral-worker.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.106",
    "@mastra/core": "^1.28.0",
    "@mastra/mcp": "^1.6.0",
    "@openrouter/ai-sdk-provider": "^2.9.0",
    "yaml": "^2.6.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  }
}
